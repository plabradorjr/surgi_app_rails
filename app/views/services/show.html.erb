  
  
  <div class="container">
    <div class="row">
      <div class="col-12 text-center">
        <br><span class="h4 text-secondary"><%=@service.name + " Service"%></span><br><br>
        </div>


      </div>
      <div class="col text-center">
        <br>
        
        <table class="table table-dark bg-light-secondary col-6 float-left">
          <thead>
            <tr>
              <th scope="col">Inventories</th>
            </tr>
          </thead>

          <tbody>
            <% @service.inventories.each do |i| %>
              <tr>
                <td scope="row"><a href="/inventories/<%=i.id%>" class="text-dark"><%= i.name.upcase %></a></td>
              </tr>
            <% end %>

          </tbody>
        </table>

        <table class="table table-dark bg-light-secondary col-6 float-right">
          <thead>
            <tr>
              <th scope="col">Memos</th>
            </tr>
          </thead>

          <tbody>
            <% @service.memos.each do |i| %>
              <tr>
                <td scope="row"><a href="/memos/<%=i.id%>" class="text-dark"><%= i.title.upcase %></a></td>
              </tr>
            <% end %>

          </tbody>
        </table>

        <br><br>

      </div>
    </div>
  </div>